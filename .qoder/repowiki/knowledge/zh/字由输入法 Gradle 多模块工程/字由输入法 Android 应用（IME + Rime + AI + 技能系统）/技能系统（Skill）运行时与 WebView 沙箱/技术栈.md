Kotlin + Android WebView（androidx.webkit.WebViewCompat），JSON 处理使用 org.json.JSONObject，网络请求通过 HttpURLConnection 手动实现 fetch 代理，协程用于 storage/fetch 异步调度（SupervisorJob + Dispatchers.Main + IO.limitedParallelism(1)）。