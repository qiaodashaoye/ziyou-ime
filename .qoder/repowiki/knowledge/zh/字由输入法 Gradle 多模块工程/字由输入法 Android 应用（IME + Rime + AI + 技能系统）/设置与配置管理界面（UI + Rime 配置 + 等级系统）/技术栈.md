Kotlin + AndroidX AppCompat（View 页面）与 Jetpack Compose（Compose 页面）混合；协程 `lifecycleScope` + `viewModelScope` 处理异步；SharedPreferences 作为轻量持久化方案；Rime 引擎通过 `core.RimeConfig` JNI 桥接；WebView 仅用于本地 Markdown 文档渲染且关闭 JS/网络。