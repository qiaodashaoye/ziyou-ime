Kotlin + Coroutines + kotlinx.coroutines.flow 作为上层 API；C++17 + JNI 直接调用 librime 静态库（`libs/${ANDROID_ABI}/librime.a`）；CMake 3.22.1 构建，启用 `--gc-sections`、`--exclude-libs,ALL` 和 16KB 页面对齐优化；通过环境变量 `RIME_USER_DATA_DIR`/`RIME_SHARED_DATA_DIR`/`RIME_DISTRIBUTION_VERSION` 注入运行时路径。