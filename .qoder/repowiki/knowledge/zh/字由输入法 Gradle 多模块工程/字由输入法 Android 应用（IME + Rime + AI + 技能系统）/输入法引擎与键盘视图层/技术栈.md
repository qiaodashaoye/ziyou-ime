Kotlin + Android InputMethodService；Rime 引擎通过 `com.ziyou.ime.daemon.RimeEngine` 暴露协程 API；UI 基于自定义 `View` 自绘（Canvas/Paint），无 XML 布局；协程 `CoroutineScope(SupervisorJob())` 管理服务生命周期；SharedPreferences + JSON 做轻量持久化；`FloatingPanelContainer` 实现悬浮窗口 insets 裁剪。