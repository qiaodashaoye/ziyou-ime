服务启动时通过 `AssetDeployer.needsDeploy` 判断是否需要 fullCheck 重新编译方案（如 t9），并异步预热 `SymbolRepository` YAML 分类缓存；剪贴板监听需在 `onCreate` 注册、`onDestroy` 注销；低内存时 `onTrimMemory` 主动关闭全部面板并调用 `RimeNative.trimNativeHeap` 归还 native 堆空闲页。