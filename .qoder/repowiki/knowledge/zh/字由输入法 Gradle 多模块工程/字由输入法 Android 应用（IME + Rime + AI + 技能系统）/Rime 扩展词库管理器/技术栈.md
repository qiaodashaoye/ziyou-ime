Kotlin + kotlinx.coroutines（`Dispatchers.IO` 协程调度）、原生 `java.net.HttpURLConnection` 进行 HTTP 请求与手动重定向处理、`org.json` 解析 JSON、`java.security.MessageDigest` 计算 SHA-256；RIME 词库格式采用 YAML + Tab 分隔词条（`.dict.yaml`）。