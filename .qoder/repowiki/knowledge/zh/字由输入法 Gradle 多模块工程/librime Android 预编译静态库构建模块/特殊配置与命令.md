首次需准备 NDK（`ANDROID_NDK_HOME` 或 `local.properties` 中的 `sdk.dir`）并执行 `cd librime-prebuilt && make librime`（等价于 `./build.sh`），可指定 ABI 如 `./build.sh arm64-v8a`；可选插件通过 `WITH_LUA=ON`、`WITH_OCTAGRAM=ON`、`WITH_PREDICT=ON` 环境变量启用；`make clean` 清理中间构建目录，`make distclean` 彻底清理产物与克隆源码。