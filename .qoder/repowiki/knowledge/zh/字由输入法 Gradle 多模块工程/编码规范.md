- 纯逻辑无 Android/JNI 依赖的代码必须下沉到 :core-logic 模块，由编译器强制 :app → :core-logic 单向依赖
- 引擎能力通过 RimeEngine/RimeApi 接口抽象并由 AppContainer 组合根注入，调用方依赖接口而非全局单例
- Rime JNI 调用统一经 RimeDispatcher 的单线程 Executor 调度，所有 API 方法为 suspend 函数
- 热路径（输入上屏）禁止直接磁盘 IO，采用内存自增 + 阈值触发异步落盘策略