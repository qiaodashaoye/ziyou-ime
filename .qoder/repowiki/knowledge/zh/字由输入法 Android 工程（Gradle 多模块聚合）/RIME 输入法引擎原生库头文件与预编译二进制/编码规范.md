- C API 通过 `RIME_FLAVORED(Type)` 宏包裹结构体名以实现版本化扩展，所有可变结构体均含 `data_size` 字段并由 `RIME_STRUCT_INIT` 初始化。
- API 函数通过 `RIME_API_AVAILABLE(api, func)` 宏在运行时检测函数指针是否可用，以支持不同版本的 librime 动态加载。
- 模块注册采用 `RIME_REGISTER_MODULE(name)` / `RIME_REGISTER_CUSTOM_MODULE(name)` 宏自动完成构造函数级别的模块注册。