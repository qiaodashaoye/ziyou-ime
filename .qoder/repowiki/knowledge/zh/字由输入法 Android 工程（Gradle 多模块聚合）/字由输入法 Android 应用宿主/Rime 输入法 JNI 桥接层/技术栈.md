C++17 + Android NDK JNI，通过 CMake 3.22.1 构建共享库，链接预编译的 `librime.a` 静态库及系统 `log` 库；使用 UTF-8 字符串处理（`utf8::unchecked::distance`），并通过 `--gc-sections`、`--exclude-libs,ALL`、`-z,max-page-size=16384` 进行 Android 15+ 兼容的链接优化。