- 所有对外工具类均以 `object` 单例形式定义，并提供带 `TAG` 常量的日志输出。
- JNI 资源操作统一采用「open → try → finally close」模式，确保 peer 句柄不会泄漏。
- Android 持久化统一通过 `Context.getSharedPreferences(name, MODE_PRIVATE)` 访问，键名以模块前缀（如 `ziyou_deploy`、`ziyou_theme`）区分。
- 配置读取失败返回 null/false 而非抛异常，由调用方决定降级策略。