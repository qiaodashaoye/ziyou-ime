Kotlin 协程（`SharedFlow`、`withTimeoutOrNull`、`SupervisorJob`）、Java `Executors.newSingleThreadExecutor` 作为 Rime 专属线程、JNI 通过 `System.loadLibrary("rime_jni")` 加载 arm64-v8a 原生库，数据对象采用 Kotlin data class 与 C++ Proto 字段对齐。