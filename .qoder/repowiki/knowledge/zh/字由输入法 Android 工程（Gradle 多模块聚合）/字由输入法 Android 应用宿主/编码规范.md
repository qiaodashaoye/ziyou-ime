- JNI 相关类集中在 core/ 包下并通过 -keep 规则保护，避免 ProGuard 混淆导致反射调用失败
- 资源部署与引擎初始化采用延迟异步策略，由 RimeSession 统一管理而非在 Application.onCreate 中同步执行
- CMake 构建参数必须与外部 librime 预编译库的模块开关保持一致（如 WITH_PREDICT）
- Compose 依赖通过 BOM 统一管理版本，避免版本冲突