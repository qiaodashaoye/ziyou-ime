# 单元测试用例数基线（全仓唯一来源，只增不减）
#
# 用途：scripts/build-release.sh 的测试门禁会从 Gradle JUnit XML 报告
#       （*/build/test-results/testDebugUnitTest/*.xml）累加实测用例数，
#       低于本文件记录的基线即判定失败。
#
# 更新方式：
#   ./gradlew :core-logic:testDebugUnitTest :app:testDebugUnitTest
#   把实测总数写到下面（首个纯数字行即基线，其余行为注释）。
#
# 当前实测：:core-logic 226 + :app 207（2026-08-11，全绿、0 跳过；
# 新增 SchemaPenaltyConfigTest 方案惩罚机制守护用例 ×6：fuzz/correction 标签、
# enable_correction 性能红线、换位规则全权重、数字映射顺序约束，防方案更新丢失权重配置）
# 当前实测：:core-logic 270 + :app 207（2026-08-12，全绿、0 跳过；
# LLM 智能预测 Phase 1 新增 core/prediction 单测 ×44：CommitWordWindowTest ×8
# 词窗口双上限/标点符号入窗、TriggerPolicyTest ×9 触发规则全分支（含扩展 A 区
# 生僻字）、ContextLruCacheTest ×7 LRU 淘汰/访问序、CandidateFusionTest ×12 引擎词
# 在前/去重/截断/exclude 防复读/标点变体归一去重、AutoPunctPolicyTest ×8 预测
# 采纳自动补标点四规则全分支）
# 当前实测：:core-logic 297 + :app 215（2026-08-13，全绿、0 跳过；
# 联想优化 Phase 0~3 新增 ×35：ContextLruCacheTest +6 扩容 256/snapshot-restore
# 往返/超容量淘汰/recentKeys 热度键、StreamCandidateTextTest ×11 流式增量解析
# （任意截断分片等价整包/仅换行切分保句读/序号与前导标点剥离/条数上限）、
# AdoptionRecordTest ×10 采纳攒批（1~4 字纯汉字词对隐私边界/双层容量淘汰/
# 往返与脏数据过滤/drain 导出）、PredictionPersistenceTest ×8 缓存与攒批的
# JSON 往返/损坏降级/原子落盘无残留）
# 当前实测：:core-logic 297 + :app 221（2026-08-13，全绿、0 跳过；
# 新增 LlmPredictionStatsTest ×6：验收指标打点结算（命中率/链式轮次/
# 预取计数/首条延迟 p50/dump 清零），支撑 §7.7 真机验证）
# 当前实测：:core-logic 305 + :app 221（2026-08-13，全绿、0 跳过；
# 联想优化实施（分析报告 §5）新增 RequestRateWindowTest ×8：滚动限流
# 记账/超限拒绝/窗口滚出释放/防抖放弃退账/清空，守护 S2 限流退账修复）
# 当前实测：:core-logic 305 + :app 223（2026-08-13，全绿、0 跳过；
# S6/S8 决策数据采集：LlmPredictionStatsTest 新增 ×2（引擎联想在场/空档
# 计数、LLM 采纳位置分桶）并更新 dump 格式断言）
# 当前实测：:core-logic 306 + :app 224（2026-08-13，全绿、0 跳过；
# 一句诗联想整首诗 Phase 2：AutoPunctPolicyTest 新增诗句链路抑制用例 ×1
#（五言/七言链不插逗号）、LlmPredictionStatsTest 新增诗词采纳计数 ×1）
# 当前实测：:core-logic 309 + :app 228（2026-08-14，全绿、0 跳过；
# 诗词联想双缺陷修复：CandidateFusionTest 新增引擎段归一去重 ×3（重复/
# 标点变体/腾位补齐）、SimpleCandidatesViewTest 新增 isMenuReplaced ×4
#（预测链 menu 替换判定，防陈旧累积追加致重复展示与点击索引错位）
# 当前实测：:core-logic 309 + :app 232（2026-08-14，全绿、0 跳过；
# 全量诗词库旁路分发：DictModelsTest ×4（catalog kind 字段向后兼容/
# predict_db 判定/未知类型防御/分类回退），守护 PredictDbManager 分流路由）
# 当前实测：:core-logic 319 + :app 232（2026-08-16，全绿、0 跳过；
# 耗电审计 P0 新增 ×10：FailureBackoffTest ×5（门槛前不阻塞/30s 冷却/
# 30→60→120 指数递增封顶/成功复位/自定义参数）、HeavyRequestGateTest ×5
#（计量网络拒绝/低电量未充电拒绝/门槛边界/充电豁免）；
# 同期收紧 TriggerPolicy 限流 800→1500ms、防抖 300→600ms、每分钟配额 20→10，
# RequestRateWindowTest 默认配额断言同步更新（用例数不变））
# 当前实测：:core-logic 322 + :app 234（2026-08-16，全绿、0 跳过；
# 九宫格退格全量回退回归修复新增 ×5：KeyRecordStackTest ×3（确认段后
# 锁拼音退格解锁保留确认段/连续分段确认跳过已有确认段/确认展示码点累计）、
# InputLogicControllerTest ×1（按键路径二次分段确认累计入栈且预览保持拼音）、
# PinyinHintProviderTest ×1（二次确认前缀累计预览），守护编码区「拼音+数字」混合形态）
# 当前实测：:core-logic 340 + :app 234（2026-08-16，全绿、0 跳过；
# AI 人设润色重构新增 ×18：PolishResultParserTest ×9（编号解析/全角分隔符
# 容忍/风格说明提取/杂散行丢弃/零编号兜底/上限截断）、Bm25IndexTest ×6
# （docFilter 打分阶段过滤/空集/null 等价/子库独立索引等价守护）、
# RagPromptBuilderTest ×4（buildPolish 润色变体：指令存在/风格参考资料/
# 编号格式与解析器对齐/预算约束））
# 当前实测：:core-logic 340 + :app 243（2026-08-18，全绿、0 跳过；
# 白霜迁移 Phase 3 新增 DictManagerFrostTest ×9：双后端主词库模板
# （FROST 四核心表/无英文表、LUNA 回滚清单、ext_dicts 注入顺序）、
# catalog v4 legacy id 幂等迁移规划（改名/零开销 no-op/二次幂等/
# 新 id 占用丢弃旧记录/混排）、deprecatedBy 向后兼容）
# 当前实测：:core-logic 340 + :app 247（2026-08-18，全绿、0 跳过；
# T9 白霜深度集成 T1 新增 PinyinHintProviderTest ×4：comment 契约变更
# 白名单防御（［］包裹丢弃/全包裹返回 null/*/∞ 标记丢弃/主路径不受影响），
# 守护 corrector 与 is_in_user_dict 挂载后的侧栏拼音提示链路）
# 当前实测：:core-logic 340 + :app 249（2026-08-18，全绿、0 跳过；
# T9 候选显示缺陷修复：PinyinHintProviderTest 语义更新 ×2（［］包裹
# 由丢弃改为归一化提取）+新增 ×2（包裹 comment 驱动预览消歧/空 comment
# 本地表回退），守护 keep_comments 读音数据链不再被切断）
# 当前实测：:core-logic 340 + :app 252（2026-08-18，全绿、0 跳过；
# T9 首候选脱钩缺陷回归新增 PinyinHintProviderTest ×3：custom_phrase
# 数字码无 comment 扫描同字数兄弟候选（64426 修复 mi'han→ni'hao）、
# 用户词星号标记扫描下一可用 comment、同字数优先防单字读音越级错位）
592
