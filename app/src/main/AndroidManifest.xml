<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 语音输入：仅本地离线识别，音频不落盘不上传（见 docs/实时语音输入可行性方案.md） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      allowBackup=false：输入法的用户词典（rime_user/）会随使用积累已输入词汇，属敏感数据。
      关闭自动备份 / adb backup 提取，避免用户输入痕迹经云备份或本地备份外泄（隐私优先）。
    -->
    <application
        android:name=".ZiyouApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:theme="@style/Theme.Ziyou">

        <!-- 输入法服务声明 -->
        <service
            android:name=".ime.ZiYouInputMethodService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/input_method" />
        </service>

        <!-- 启动页：输入法启用引导（检测启用/激活状态，就绪后才可进入设置页） -->
        <activity
            android:name=".ui.ImeSetupActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 设置页面（仅在输入法已启用并激活后可进入，见 ImeSetupActivity 门禁） -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- 首启偏好向导（首次完成输入法启用与激活后展示，收集初始输入偏好） -->
        <activity
            android:name=".ui.UserPreferenceSetupActivity"
            android:exported="false"
            android:label="初始偏好设置" />

        <!-- 扩展词库管理 -->
        <activity
            android:name=".ui.DictManagerActivity"
            android:exported="false"
            android:label="扩展词库" />

        <!-- 技能插件管理 -->
        <activity
            android:name=".ui.SkillManagerActivity"
            android:exported="false"
            android:label="技能插件" />

        <!-- 技能开发文档 -->
        <activity
            android:name=".ui.SkillDevGuideActivity"
            android:exported="false"
            android:label="技能开发文档" />

        <!-- AI 知识库管理（导入/删除/同步知识条目） -->
        <activity
            android:name=".ui.KnowledgeActivity"
            android:exported="false"
            android:label="AI 知识库" />

        <!-- 等级体系详情 -->
        <activity
            android:name=".ui.LevelActivity"
            android:exported="false"
            android:label="我的等级" />

        <!-- 皮肤管理（预览/切换/导入/自定义） -->
        <activity
            android:name=".ui.SkinActivity"
            android:exported="false"
            android:label="键盘皮肤" />

        <!-- 皮肤开发文档 -->
        <activity
            android:name=".ui.SkinDevGuideActivity"
            android:exported="false"
            android:label="皮肤开发文档" />

        <!--
          FileProvider：把 cache/ime_images/ 下的待发送图片（涂鸦导出 / AI 答案转图 /
          技能图片输出）暴露为 content:// URI，
          经 commitContent(INPUT_CONTENT_GRANT_READ_URI_PERMISSION) 临时授权给目标应用读取。
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.imecontent"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/ime_file_paths" />
        </provider>

    </application>
</manifest>
